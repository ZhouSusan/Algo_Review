Coding Challenge: Optimize this algorithm. 

We have an algorithm called findMaximum. 

This finds the largest of three numbers. 

If there are duplicate entries, we still return whatever the maximum is.